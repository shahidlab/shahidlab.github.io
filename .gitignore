
# Jekyll build output -- rebuilt fresh by the GitHub Actions Pages workflow,
# shouldn't be committed (was previously tracked, causing noisy diffs on
# every local build).
_site/
.jekyll-cache/
.sass-cache/

# macOS Finder metadata -- a bare pattern (no leading/trailing slash)
# matches this filename at any depth, so one line covers every directory.
.DS_Store
